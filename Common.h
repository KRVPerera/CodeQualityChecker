#include <vector>
#include <algorithm>
#include <set>
#include <map>
#include <cmath>
#include <chrono>

#ifndef COMMON_H
#define COMMON_H

typedef std::vector<double> TimesVector;
typedef std::pair<int,bool> IntBool;
typedef std::set<int> IntSet;
typedef std::map<int, bool> IntBoolMap;
typedef std::vector<int> IntVec;

#endif // COMMON_H
